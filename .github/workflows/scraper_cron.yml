name: Football Results Grabber

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Il workflow e attivo!"